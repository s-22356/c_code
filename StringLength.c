#include<stdio.h>
#include<string.h>
main()
{
    char str[20]={'s','u','d','i','p','p'};
    int i,count=0;
    for(i=0;i<=str[i];i++)
    {
        printf("%c",str[i]);
        count++;
    }
    printf("The length of array is :: %d ",count);
    getch();
}

